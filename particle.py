# -*- coding: utf-8 -*-

import math
import numpy as np

class particle:
	def __init__(self,):
        pass

    def verletInitial(self)
    def verlet(self)





def verletInitial(self,forces):
    dx_p = self.dx + forces * const.DTD**2
    self.x = self.x + self.dx 
    SELF.V = (dx_p + self.dx)/(2.*DTD)
    self.dx = dx_P
    
def verlet(self,forces):
    dx_p = self.dx + forces * const.DTD**2
    self.x = self.x + self.dx 
    SELF.V = (dx_p + self.dx)/(2.*DTD)
    self.dx = dx_P
        
